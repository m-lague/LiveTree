
  <div class="index-header">
    <h1 class="index-header__title">
      All</h1>
      <div class="index-header__network__container">
      <div class="index-header__title__network">Facebook</div>
      <div class="index-header__title__network">Instagram</div>
      <div class="index-header__title__network">Tiktok</div>
      <div class="index-header__title__network">Youtube</div>
      <div class="index-header__title__network">Snapchat</div>
      </div>
         <h1 class="index-header__title">Lives in One Place.
    </h1>

    <div class="index-header__cta">
      <% if current_user %>
        <%= link_to "Add my next Live", new_live_path, class: "btn btn--primary" %>
      <%else%>
        <%= link_to "Sign up", new_user_registration_path, class: "btn btn--primary" %>
      <%end%>
    </div>
  </div>

  <div class="cards-container">
    <%= render @lives %>
  </div>
